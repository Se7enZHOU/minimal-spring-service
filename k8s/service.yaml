apiVersion: v1
kind: Service
metadata:
  name: minimal-spring-service
  labels:
    app.kubernetes.io/name: minimal-spring-service
spec:
  selector:
    app.kubernetes.io/name: minimal-spring-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
